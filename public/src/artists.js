
//like artists/songs
const icons = document.querySelectorAll(".artist-box");
//icons.forEach(icon => {
  //  icon.addEventListener('click', () => icon.classList.toggle("liked"));
  //});

/*
//admin delete 
  const deleteButtons = document.querySelectorAll(".eraseBtn");
  const deleteArtists = document.querySelectorAll(".artist-box");

  for (let i = 0; i < deleteButtons.length; i++) {
    deleteButtons[i].addEventListener("click",()=>deleteArtists[i].remove());
  }*/